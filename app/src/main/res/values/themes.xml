<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme. Compose (Material 3) draws all in-app UI, so this only needs
         to be a no-action-bar theme with a sane window background. Edge-to-edge
         and system-bar colours are configured at runtime in MainActivity. -->
    <style name="Theme.TransitLine" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <!-- Splash screen theme (androidx.core:core-splashscreen). -->
    <style name="Theme.TransitLine.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.TransitLine</item>
    </style>
</resources>
